{
  "name": "telegram-drive-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root wrapper for the Telegram Drive website in app/.",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "preview": "npm --prefix app run preview"
  }
}
